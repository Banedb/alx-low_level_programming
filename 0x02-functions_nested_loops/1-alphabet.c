#include "main.h"
/**
 * print_alphabet - prints the alphabets in lower case
 * takes no argument
 *
 * Returns no value
 */
void print_alphabet(void)
{
	char alf = 'a';

	while (alf <= 'z'){
		_putchar(alf);
		alf++;
}
}
